var num = 0;//loop variable
while(num<5){ // loop condition
    console.log('hello');// loop statement
    num = num + 1; // loop variable change
// num += 1;//same process but different
//num++;// same process but different
}
//
var num = 0; // Initialize the variable 'num' to 0

// Start a while loop that will continue as long as 'num' is less than 5
while(num < 5) {
    console.log('hello'); // Print 'hello' to the console

    num = num + 1; // Increment 'num' by 1 after each iteration
}

// The loop will run 5 times, printing 'hello' each time, until 'num' becomes 5 and the loop condition (num < 5) is no longer true.
